class Solution {
public:
    vector<int>ans;
    void helper(TreeNode* root)
    {
        if(root==NULL)return;
        helper(root->left);
        ans.push_back(root->val);
        helper(root->right);
        return;
    }
    vector<int> inorderTraversal(TreeNode* root) {
        helper(root);
        return ans;
    }
};
