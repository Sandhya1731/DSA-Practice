// https://www.geeksforgeeks.org/write-a-program-to-reverse-an-array-or-string/


// Divide the string into two parts and swap each element of first part ith with the elements of second part N-i-1
// can be implemented in iterative and reccursive manner. (TC-O(n))
class Solution
{
    public:
    string reverseWord(string str)
    {
        // Your code goes here
        int n=str.size();
        for(int i=0;i<n/2;i++){
            int temp=str[i];
            str[i]=str[n-i-1];
            str[n-i-1]=temp;
        }
        return str;
    }
};
